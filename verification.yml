### python3 env for everything in this repo.
###
###   conda env create -f verification.yml
###   conda activate verification
###
### Left unpinned on purpose -- pinned builds went stale and stopped importing.

name: verification
channels:
  - conda-forge
dependencies:
  - python>=3.10
  - beautifulsoup4
  - click
  - jupyterlab
  - loguru
  - numpy
  - pandas
  - requests
